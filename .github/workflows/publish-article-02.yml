name: Refresh Article 02 current network evidence

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-article-02-network-state
  cancel-in-progress: false

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Migration guard
        run: echo "The current-network evidence workflow is being upgraded and will be re-enabled in the final commit."
