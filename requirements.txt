# Reuse the pinned evidence environment required to verify the immutable
# 100-record MOLNFT reconstruction audit.
-r requirements.lock

# Current publication and graphics dependencies.
cairosvg>=2.7,<3
Pillow>=10,<12
playwright>=1.46,<2
mistune>=3.1,<4
